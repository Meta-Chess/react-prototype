//NOTE: It's important we have the form ruleName + "Settings" as the exported name
import { RULE_SETTINGS as chainReactionSettings } from "./chainReactionSettings";
export { chainReactionSettings };

import { RULE_SETTINGS as threeCheckSettings } from "./threeCheckSettings";
export { threeCheckSettings };
